# React App Environment Variables

# Backend URL (if not set, defaults to http://localhost:5001)
# REACT_APP_BACKEND_URL=http://localhost:5001

# Webpack Dev Server Configuration
# Fix WebSocket connection errors by setting these:
WDS_SOCKET_HOST=localhost
WDS_SOCKET_PORT=3000
WDS_SOCKET_PATH=/ws

# Or disable HMR if you don't need hot reloading:
# FAST_REFRESH=false
